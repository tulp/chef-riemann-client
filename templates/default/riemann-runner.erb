require 'rubygems'        # if you use RubyGems
require 'daemons'


options = {
    :app_name   => "<%=@app_name%>",
    :ARGV       => ['start', '-f', '--'],
    :dir_mode   => :system,
    :dir        => 'pids',
    :multiple   => false,
    :ontop      => true,
    :mode       => :load,
    :backtrace  => true,
    :monitor    => false
}
Daemons.run("<%=@executable%>", options)