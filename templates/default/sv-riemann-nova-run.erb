#!/bin/sh

exec 2>&1
ulimit -n 4096
ulimit -a

export RBENV_VERSION=<%= node[:riemann][:ruby_version] %>
exec chpst -u root <%= node[:riemann][:ruby_exec]%> <%=node[:riemann][:nova][:riemann_executable]%> --host <%= @options[:riemann_host] %> -e <%= @options[:name]%>
