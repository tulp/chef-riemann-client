#!/bin/sh

exec 2>&1
ulimit -n 4096
ulimit -a


exec chpst -u nobody /opt/vagrant_ruby/bin/riemann-health --host <%= @options[:host] %> -e <%= node[:network][:interfaces][:eth1][:addresses].keys.first%>